#include <iostream>

using namespace std;

int main() {
    int arr[3];
    int N = 0;

    while (cin >> arr[N]) {
        N++;
        cout << N << endl;
    }

    // Proses menggunakan array arr[] dengan N elemen

    return 0;
}
