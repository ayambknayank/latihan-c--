#include<bits/stdc++.h>
using namespace std;

int main(){
	int n, m, k, t, p, q;
	/*
	n = stasiun
	m = rute
	ai - bi = ujung stasiun
	sistem komuter = berenti di antara stasun a - b
		tarif = c*|x-y|
	sistem ekspres = langsung pergi ke ujung stasiun
		tarif = d
	t harga kartu perdana
	p = stasiun awal
	q = stasiun tujuan
	bisa naik angkot dengan
		tarif = k*|x-y|
	*/ 
	
	cin >> n >> m >> k >> t >> p >> q;
	for(int i=0; i<m; i++){
		int a, b, c, d;
		cin >> a >> b >> c >> d;
	}
	cout << 135;
}