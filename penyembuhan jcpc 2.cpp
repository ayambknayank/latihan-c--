#include<bits/stdc++.h>
using namespace std;

string LCS(string s, string t){
	int slen = s.length();
	int tlen = t.length();
	int dp[slen][tlen];
	for(int i=0; i<slen; i++){
		for(int j=0; j<tlen; j++){
			dp[i][j]=
		}
	}
}

int main(){
	string s, t, x;
	cin >> s >> t >> x;
	
	string lcs = LCS(a, t);
	
}