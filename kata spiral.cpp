#include<bits/stdc++.h>
using namespace std;

int main(){
	string s;
	getline(cin, s);
	int len = s.length();
	vector<vector<int, int>> tabel(len, vector<int> (len, 0));
	for(int i=len/2; i<len; i++){
		for(int j=len/2; j<len; j++){
			if(tabel[i][j]!=0){
				
			}else{
				if()
			}
		}
	}
}