#include <bits/stdc++.h>
using namespace std;
#define bismillah ios::sync_with_stdio(false), cout.tie(0), cin.tie(0);

int main() {
    bismillah

    int N, K;
    cin >> N >> K;

    vector<int> A(N);
    for (int i = 0; i < N; ++i) {
        cin >> A[i];
    }
    cout << 1 << endl;

    return 0;
}